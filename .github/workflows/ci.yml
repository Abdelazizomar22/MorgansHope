name: CI

on:
  pull_request:
  push:
    branches:
      - main
      - abdelaziz/**

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - project: frontend
            install_command: npm ci --no-audit --no-fund
          - project: backend
            install_command: npm install --include=dev --omit=optional --no-audit --no-fund
    defaults:
      run:
        working-directory: ${{ matrix.project }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: ${{ matrix.project }}/package-lock.json

      - run: ${{ matrix.install_command }}
      - run: npm run build
